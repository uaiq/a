GitHub Portfolio — uaiq

This is a minimal black & white portfolio site that lists public repositories from https://github.com/uaiq/.

Open `index.html` in a browser to view. To serve locally (recommended), run:

```bash
python -m http.server 8000
```

Then open http://localhost:8000/ in your browser.
